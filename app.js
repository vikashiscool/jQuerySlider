(function( $ ) {
 
    $.fn.slider = function( action ) {
 
        if ( action === "hover") {
            // Open popup code.
        }
 
        if ( action === "slide" ) {
            // Close popup code.
        }
 
    };
 
}( jQuery ));